<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- 填充的颜色： -->
    <solid android:color="@color/transparent" />
    <!-- 边框的颜色 ：不能和窗口背景色一样-->
    <stroke
        android:width="@dimen/margin_width1"
        android:color="@color/red" />
    <!-- padding：Button里面的文字与Button边界的间隔 -->
    <padding
        android:bottom="@dimen/margin_width2"
        android:left="@dimen/margin_width2"
        android:right="@dimen/margin_width2"
        android:top="@dimen/margin_width2"/>
</shape>